# Aether Explorer does not require project-level cloud secrets by default.
# Configure AI providers inside the app settings if you opt in to AI features.
#
# This file is intentionally minimal so local-first builds do not encourage
# storing API keys in repository-level environment files.
