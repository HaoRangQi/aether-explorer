# Plan

## Build

- Clean previous release bundle artifacts.
- Run Tauri production build for `app` and `dmg` bundles with updater artifacts disabled.

## Verification

- Confirm artifact paths exist.
- Run basic macOS app bundle validation.
- Record signing identity caveat.
