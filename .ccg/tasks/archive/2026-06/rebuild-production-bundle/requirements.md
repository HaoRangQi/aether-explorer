# Requirements

- Rebuild a local production-style macOS package from the current working tree.
- Produce `.app` and `.dmg` artifacts for user inspection.
- Avoid full release automation that requires updater keys, GitHub authentication, or Developer ID credentials.
- Do not stage or commit business source changes.

